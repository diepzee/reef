---
kind: changed
---
Everything reef calls a shared memory is now a cove, in the app, the tools
your assistant uses, and the CLI. If you have scripts or saved prompts that
name a space — `list_spaces`, a `space` argument, `reef list-spaces` — they
need the cove spelling now.
