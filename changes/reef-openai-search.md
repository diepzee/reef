---
kind: added
---
reef now works in clients that only allow two tools — ChatGPT connectors
outside developer mode, and company-knowledge sources — by answering to
`search` and `fetch` alongside its full set.
