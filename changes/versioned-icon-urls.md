---
kind: fixed
---
A new reef mark now actually reaches your assistant. The icon was advertised
at a fixed address, so a client that had already fetched one was entitled to
keep showing it; each release now points at a fresh address instead.
