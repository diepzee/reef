.worktrees/
.superpowers/
__pycache__/
.venv/
clients/ts/node_modules/
clients/ts/dist/
clients/ts/dist-test/

# Local secrets. Nothing in the repo reads these today (settings come from the
# real environment, and Railway injects them in production) -- this is here so
# that a token pasted into a .env during local work cannot be committed.
.env
.env.*
!.env.example
