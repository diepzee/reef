# Repo metadata for going public

Set on `github.com/diepzee/rif` via `gh repo edit` while the repo is still
private, so it's already in place when it flips to public.

## Description

```
reef — shared, living memory for you and your people, over MCP
```

## Homepage

```
https://reefwith.me
```

## Topics

```
mcp
mcp-server
memory
claude
chatgpt
ai-memory
model-context-protocol
```

## Social preview image

GitHub's social preview (used for link unfurls on Slack, X, etc.) is not
settable via `gh` or the API — it must be uploaded manually.

- Go to **Settings → General → Social preview** on the repo.
- Upload `social-preview.png`, with the brain fan on deep teal, at 1280×640.
- This step is still outstanding as of this writing.
